# AI常駐PMO設計書 — プライベートナッジによるプロジェクト推進支援

作成日: 2026-07-17
ステータス: 構想・設計段階（実装未着手）

AI時代の「プロジェクト推進ツール」の中核仮説 —— **AIが善いPMOのように常駐し、裏方としてプロジェクトを前に進める** —— を要求と設計に落としたもの。

「善い」の定義は色々ありうるが、本設計では **don't be evil** を最低線とする: このAIは監視・査定・炙り出しの道具には決してならず、常に本人の側に立つ。§2.3「やらないこと」はその具体化である。

戦略レベルの背景（3つの逆転・Cairn の座取り）は [`docs/ai-era-pm-strategy.md`](./ai-era-pm-strategy.md) を参照。本書はそのうち「逆転② 視線」の実装設計にあたる。

[`docs/10_ai_member_design.md`](./10_ai_member_design.md) の Stage 3「心拍を持つ（半自動）」を具体化・先行実装する位置づけ。ただし Stage 3 が想定する「チャンネルでの公開発言」とは異なり、本設計は**本人だけに見えるプライベートナッジ**を配信の第一形態とする（理由は §1.2）。

---

## 1. コンセプト

### 1.1 「PMO常駐」とは

- AIが定期的にワークスペースを巡回（ハートビート）し、プロジェクトの停滞・リスクの兆候を検知する
- 検知したら、**関係する本人にだけ**チャット欄の文脈の中でそっと知らせる
- 例:「タスク『装備リスト確定』の期限が明後日ですが、まだ着手されていないようです」

### 1.2 プライベートナッジが第一形態である理由

この種の機能が失敗する最大の理由は、検知精度ではなく**リマインドが公開処刑になること**にある。遅れている人を全員の前で名指しすれば、ツールは「監視装置」として嫌われ、最悪の場合チャットに本音が書かれなくなる（検知の材料自体が消える）。

そこで:

- ナッジは宛先本人にのみ表示し、「**このメッセージはあなただけに見えています**」を常に明示する
- 善いPMOが廊下でそっと声をかけるのと同じ振る舞いを、プロダクトの原則として固定する
- チーム全体に関わるリスクの公開発言（Stage 3 本来のスコープ）は、プライベートナッジで信頼を獲得した後の後続フェーズとする

### 1.3 成否を分けるのは「言わない判断」

的外れな指摘を2〜3回されたユーザーはナッジをミュートし、機能は形骸化する。本設計では検知アルゴリズムと同等以上に、**誤指摘率と鬱陶しさの制御**（重複抑止・クールダウン・頻度上限・フィードバック学習）を一級の設計対象とする。

---

## 2. 要求

### 2.1 機能要求

| # | 要求 | 備考 |
|---|---|---|
| F-1 | 定期巡回（ハートビート）でワークスペースの状態を検査する | Inngest cron |
| F-2 | 「期限が近い / 過ぎているのに未着手」のタスクを検知し、担当者本人にナッジを送る | ルールベース（Phase 1） |
| F-3 | 「進行中のまま長期間更新がない」タスクを検知し、担当者本人にナッジを送る | ルールベース（Phase 1） |
| F-4 | チャットの流れから潜在リスク（結論が出ずに流れた議論・認識齟齬の兆候・未回答の依頼）を検知し、関係者本人にナッジを送る | LLM巡回（Phase 2） |
| F-4b | **宛先復元ナッジ**: 宛先が特定されないまま未回答の質問・依頼について、最も答えられそうな人を推定し、その本人にだけ「この質問、あなたが一番答えられそうです」と知らせる | LLM巡回（Phase 2）。§5.2 参照 |
| F-5 | ナッジは該当チャンネルのチャット欄に「あなただけに見えています」の明示付きで表示する | タイムラインへの合成表示 |
| F-6 | ナッジから対象（タスク・メッセージ）へ1タップで遷移できる | |
| F-7 | ナッジに対して「あとで」「これは問題ない」のフィードバックを返せる | 抑止学習の入力 |
| F-8 | 同一事象への指摘を繰り返さない（重複抑止・クールダウン） | dedupe キー |
| F-9 | ナッジ生成理由（どの検知器が何を根拠に発火したか）を記録する | デバッグ・信頼性検証用 |
| F-10 | 過去の経緯を添えた文脈付きナッジ（「この議論は2週間前に同じ結論が出ています」） | メッセージ埋め込み（Phase 3） |
| F-11 | **個人キルスイッチ**: ユーザー本人がナッジを一括オフにできる | Phase 1 必須。§8.3 参照 |

### 2.2 非機能要求

| # | 要求 | 備考 |
|---|---|---|
| N-1 | **沈黙がデフォルト**。確信度が低い場合は発言しない | [`10_ai_member_design.md`](./10_ai_member_design.md) §5 の原則を継承 |
| N-2 | 頻度上限: 1ユーザーあたり1日 N 件まで（初期値 3） | 超過分は破棄（キューに溜めない。翌日には状況が変わっているため） |
| N-3 | AIの閲覧範囲は既存の権限モデルに完全準拠する | §7 参照 |
| N-4 | DM チャンネルは巡回対象外 | §7 参照 |
| N-5 | LLM コストは差分巡回 + gpt-4o-mini 一次判定で抑える | §5.2 参照 |
| N-6 | 静寂時間帯（深夜・早朝）にはナッジを配信しない | 生成はしてよいが配信を遅延 |

### 2.3 やらないこと（設計上の禁止事項）

- **公開の場での催促・名指し**。ナッジは本人以外に一切表示しない
- **管理者・リーダー向けの「遅れている人一覧」**。ナッジの存在・内容・既読状態を本人以外（owner / admin を含む）に見せる機能は作らない。集計・レポートも作らない。「炙り出しに使えない」ことがこの機能の信頼の根拠であり、要望があっても原則として拒否する
- **自動エスカレーション**。ナッジを無視し続けても、AIが勝手にリーダーへ報告することはしない（本人に「チームに共有しましょうか?」と提案するに留める）
- **DM の監視・巡回**
- **ナッジからの直接的な状態変更の自動実行**。タスク操作は本人の明示的なアクションを経る

---

## 3. 全体アーキテクチャ

```
Inngest cron（ハートビート）
  │
  ├─ Phase 1: ルール検知（SQL）
  │    tasks.dueDate × status × assigneeId → 候補
  │
  ├─ Phase 2: LLM 巡回（差分メッセージ）
  │    前回スキャン以降の新規メッセージがあるチャンネルのみ
  │    gpt-4o-mini 一次判定 → 発話候補のみ gpt-4o で精査
  │
  ▼
発話ゲート（言わない判断）
  重複抑止（dedupeKey）→ クールダウン → 頻度上限 → 静寂時間帯 → 確信度
  │
  ▼
ai_nudges 行を INSERT
  │  DB トリガー + realtime.broadcast_changes()（既存の Broadcast from Database 方式）
  ▼
本人のクライアントだけが invalidate → REST 再取得
  │
  ▼
チャット欄に「あなただけに見えています」カードを合成表示
  └─ フィードバック（あとで / 問題ない）→ 抑止学習へ還流
```

新しいインフラは不要。Inngest（導入済み）・Supabase Realtime（Broadcast from Database、導入済み）・pgvector（`document_chunks` 運用中）の組み合わせで成立する。

---

## 4. データモデル

### 4.1 `ai_nudges`（新規）

ナッジは**通常の `messages` 行にしない**。messages は Realtime でチャンネル全員に配信される前提であり、「本人だけに見える」を messages の上に実装すると配信・未読・既読の全経路に分岐が入るため。宛先ユーザーを持つ独立テーブルとし、チャットUI側でタイムラインに合成する。

```
ai_nudges
  id            uuid PK
  workspace_id  uuid NOT NULL → workspaces
  user_id       uuid NOT NULL → profiles        -- 宛先。本人のみ閲覧可（RLS / API 双方で強制）
  channel_id    uuid → channels    ON DELETE SET NULL  -- チャット欄のどこに出すか（プロジェクトチャンネル）
  project_id    uuid → projects    ON DELETE SET NULL
  task_id       uuid → tasks       ON DELETE SET NULL  -- 対象タスク（ルール検知時）
  message_id    uuid → messages    ON DELETE SET NULL  -- 対象メッセージ（LLM検知時の根拠）
  detector      text NOT NULL                   -- 'task_due_soon' | 'task_overdue' | 'task_stalled' | 'unanswered_ask' | 'llm_risk'
  dedupe_key    text NOT NULL                   -- 再発防止キー（§6.1）
  title         text NOT NULL
  body          text NOT NULL
  reason        jsonb NOT NULL DEFAULT '{}'     -- 発火根拠（検知器の入力スナップショット。F-9）
  status        text NOT NULL DEFAULT 'active'  -- 'active' | 'dismissed' | 'resolved' | 'suppressed'
  feedback      text                            -- 'later' | 'not_helpful' | null
  remind_after  timestamptz                     -- この時刻まで再 active 化しない（§6.1 の状態リコンサイル）
  created_at    timestamptz NOT NULL DEFAULT now()
  responded_at  timestamptz

  UNIQUE (user_id, dedupe_key)
  INDEX (user_id, status, created_at)
  INDEX (channel_id, user_id)
```

- **ナッジ行は「イベント」ではなく「その関心事の現在状態」**として扱う。`(user_id, dedupe_key)` が一意で、ハートビートは新規 INSERT を積むのではなく、既存行の `status` をリコンサイル（再評価）する（詳細は §6.1）
- `status: 'resolved'` は対象の解消（タスク完了・期限変更）をハートビートが検知して立てる。ユーザー操作なしで消えるナッジは「AIがちゃんと見ている」体験になる
- `feedback: 'not_helpful'` は同一 `detector × 対象` の長期クールダウンに使う（§6.2）
- `task_id` / `message_id` / `channel_id` / `project_id` は全て `ON DELETE SET NULL` とする。デフォルトの `NO ACTION` のままだと、ナッジが1件でも紐づいたタスクは `DELETE FROM tasks` が失敗し（例: `apps/web/src/app/api/tasks/[id]/route.ts` のタスク削除エンドポイントが直接 `db.delete(tasks)` するため）、通常のタスク削除操作を壊してしまう。参照先が消えた古いナッジは表示上「対象は削除されました」等にフォールバックする

### 4.2 `ai_scan_states`（新規・Phase 2）

LLM巡回の差分カーソル。チャンネルごとに「どこまで読んだか」を持ち、新規メッセージがあるチャンネルだけを巡回対象にする。

```
ai_scan_states
  channel_id            uuid PK → channels    ON DELETE CASCADE
  last_scanned_message_id  uuid → messages    ON DELETE SET NULL
  last_scanned_at       timestamptz NOT NULL

```

- `channel_id` は `ON DELETE CASCADE` 必須。デフォルトの `NO ACTION` のままだと、一度でも巡回したチャンネルが削除不能になる。`channels.project_id` は既に `ON DELETE CASCADE` でプロジェクト削除時にチャンネルへ連鎖するため、ここで塞がないとプロジェクト削除フロー自体が壊れる

### 4.3 `document_chunks` の拡張（Phase 3）

`sourceType` に `'message'` を追加し、チャットメッセージを埋め込み対象にする。巡回には必須ではなく（§5.2）、ナッジへの文脈付与・過去経緯の検索（F-10）で効く。粒度は1メッセージ単位ではなく「同一チャンネル・近接時間帯のまとまり」でチャンク化する（短文が多くベクトルが痩せるため）。

**アクセス制御上の注意**: 既存の `document_chunks` にはチャンネル識別子がなく、`searchChunks` は `workspaceId` 単位でしか絞れない（ゲスト向けのフィルタも `project` / `file` チャンクの扱いしか知らない）。`message` チャンクを追加する際は `metadata` に `channelId`（+ プライベートチャンネルなら判定に必要な情報）を必ず埋め込み、`searchChunks` 側もプライベートチャンネル・プロジェクトチャンネルの guest 制限を `message` チャンクに対して適用するよう拡張する。これを怠ると、参加していないプライベートチャンネルの発言が `/api/ai/conversations` の RAG コンテキスト経由で他メンバーに漏れる。

---

## 5. 検知設計

### 5.1 Phase 1 — ルールベース検知（LLM不要）

`tasks` / `projects` の構造化データだけで高精度に判定できる領域。誤指摘がほぼゼロのため、**ここで「裏方PMO」の体験・配信基盤・発話ゲートを確立する**。

| detector | 条件（初期値） | 宛先 | dedupe_key |
|---|---|---|---|
| `task_due_soon` | `today <= dueDate <= today + 3日` かつ `status = 'todo'` かつ `assigneeId IS NOT NULL` | 担当者 | `task_due_soon:{taskId}:{dueDate}` |
| `task_overdue` | `dueDate < today` かつ `status != 'done'` かつ `assigneeId IS NOT NULL` | 担当者 | `task_overdue:{taskId}:{dueDate}` |
| `task_stalled` | `status = 'in_progress'` かつ `updatedAt < now - 7日` かつ `assigneeId IS NOT NULL` | 担当者 | `task_stalled:{taskId}:{ハートビート実行日の週}` |

- 3検知器とも `assigneeId IS NOT NULL` を必須とする。`ai_nudges.user_id` は NOT NULL のため、担当者未設定のタスクは送り先が存在せず対象から除外する（未アサインタスクの停滞は別途「担当者を決めましょう」という文脈だが、初期スコープには含めない）
- `task_due_soon` に `dueDate >= today` の下限を設け、`task_overdue` と排他にする。下限がないと期限超過タスクが両方の条件を満たし、dedupe_key が異なるため2件のナッジが飛んで1日3件の枠を無駄に消費する
- dedupe_key に `dueDate` を含めるため、期限が変更されたら（=状況が変わったら）再度ナッジできる。`task_stalled` の週は **タスクの `updatedAt` ではなくハートビート実行日**から算出する。`updatedAt` 基準だと、最初のナッジ後もタスクが一切触られなければ `updatedAt` が変化し続けないため dedupe_key が永久に同じになり、UNIQUE制約が2回目以降の再送を封じてしまう（§6.1 の「新しい停滞週になれば再送可能」という前提と矛盾する）
- ナッジの `channel_id` は対象タスクのプロジェクトチャンネル。ハートビートは 1日1回、各ワークスペースの朝（初期値 09:00 JST）に実行
- 閾値（3日・7日）は将来ワークスペース設定化する。初期はコード定数でよい

### 5.2 Phase 2 — LLM巡回（潜在リスク検知）

**チャットのベクトル化は巡回には使わない。** 巡回は「前回スキャン以降の新規メッセージ」を時系列のまま読ませる方が確実で、差分だけ読むためコストも抑えられる。

1. `ai_scan_states` を参照し、新規メッセージのあるチャンネル（DM除く）を列挙
2. チャンネルごとに直近ログ（新規分 + 文脈用の直前数十件）を **gpt-4o-mini** にPMOプロンプトで渡し、一次判定:
   - 結論が出ないまま流れた議論はないか
   - 未回答のままの依頼・質問はないか
   - 認識齟齬・スコープ膨張の兆候はないか
3. 候補が出たときだけ **gpt-4o** で精査し、確信度・宛先・文面を生成。確信度がしきい値未満なら破棄（沈黙がデフォルト）
4. 発話ゲート（§6）を通過したものだけ `ai_nudges` に INSERT

巡回頻度は6時間おき程度から開始し、コストと検知の鮮度を見て調整する。宛先の決定は「その事象に行動を起こせる本人」（依頼を受けた人・議論の当事者）に限定し、特定できない場合は発話しない。

#### 宛先復元ナッジ（detector: `unanswered_ask`）

チャットの沈黙は速度の問題ではなく**責任の拡散**である。対面で話が進むのは目が合った人が答えざるを得ないから——つまり効いているのはペースではなく**宛先の特定**。宛先のない問いは誰のタスクでもないため、誰も返さない。

そこで、宛先が曖昧なまま一定時間（初期値 24時間）未回答の質問・依頼を検知し、**最も答えられそうな一人**にだけナッジを送って宛先を復元する:

> この質問、あなたが一番答えられそうです（このメッセージはあなただけに見えています）

- 宛先の推定材料: 関連タスクの担当者、当該トピックでの過去の発言、メンバーのスキル・経験（`document_chunks` の `sourceType: 'member'`）
- 推定の確信度が低い場合は発話しない（沈黙がデフォルト）。**候補者全員に一斉送信することは絶対にしない**——それは責任の拡散をAIが再生産するだけ
- dedupe_key: `unanswered_ask:{messageId}`
- 「全員が読んで様子見している熟考の沈黙」は健全なので触らない。対象は「回答がないと進行がブロックされる問い」に限定する
- 全チャンネルを対面のペースで進めることが目的ではない。チャンネル参加は物理的制約を受けないため、全応答を強いれば人は無限にタイピングし続けることになる。ナッジの頻度上限（§6.3）が実質的な**注意の予算**として働き、「進行を本当にブロックしている少数の待ちだけを、解消できる一人に、予算内で届ける」ことを保証する

### 5.3 Phase 3 — メッセージ埋め込みによる文脈付与

`document_chunks` に `sourceType: 'message'` を追加し、ナッジ生成時に関連する過去の議論をベクトル検索で引いて文面に添える。

- 例:「この論点は 6/28 の議論で『A案で進める』と一度決まっています」
- 既存の `/api/ai/conversations`（AIチャット）の検索コンテキストにもそのまま流用でき、投資が二重に効く

---

## 6. 発話ゲート（言わない判断）

ナッジ候補は以下を**すべて**通過した場合のみ配信する。

### 6.1 重複抑止（状態リコンサイル方式）

ナッジは append-only なイベントログではなく、`(user_id, dedupe_key)` を主キーとする**関心事の現在状態**として扱う。ハートビートは毎回条件を再評価し、既存行の `status` をリコンサイルする（新規 INSERT を積み増さない）。これにより「同一事象を二度言わない（§6.1）」と「あとで、で2日後に再度言う（§6.2）」が単一の行の状態遷移として無矛盾に両立する。

各ハートビートでの `(user_id, dedupe_key)` ごとの処理:

| 現在の行 | 条件が継続 | 条件が解消 |
|---|---|---|
| 行なし | **横断クールダウンチェック**（下記）を経て `active` で INSERT（`ON CONFLICT DO NOTHING` は競合保険） | 何もしない |
| `active` | そのまま（既に表示中。再通知しない） | `resolved` にする |
| `dismissed` / `suppressed` かつ `remind_after` 到来 | `active` に戻す（= 再ナッジ。新規配信として扱い、日次上限を1消費し Realtime UPDATE を配信） | `resolved` にする |
| `dismissed` / `suppressed` かつ `remind_after` 未到来 | そのまま抑止 | そのまま |
| `resolved` | そのキーでは終了（新しい期限・新しい停滞週は §5.1 のとおり別 key になり別行として立つ） | — |

- `remind_after` により再 active 化のタイミングを制御する。`dedupe_key` は変えない
- 状況が本質的に変わる（期限変更・新しい停滞週）と §5.1 の設計どおり key 自体が変わり、別の関心事として新規行が立つ

**横断クールダウンチェック**: `task_stalled` の dedupe_key は「ハートビート実行日の週」を含んで週ごとに回転する（§5.1）。ある週の行に `not_helpful`（`remind_after = now + 30日`）を付けても、翌週は別の `dedupe_key` を持つ新規行になるため、行単位の `remind_after` だけでは30日抑止をすり抜けてしまう。これを防ぐため、**「行なし」で新規 INSERT する前に、同一 `(user_id, detector, task_id)` の組で `status = 'suppressed'` かつ `remind_after > now()` の行が既に存在しないかを確認し、あれば INSERT をスキップする**（`dedupe_key` が変わっても `detector` と `task_id` は同一タスクを指し続けるため、この2列で対象を横断的に同定できる。新カラムは不要）。`task_due_soon` / `task_overdue`（key に日付を含むが週のように繰り返し回転はしない）や `unanswered_ask` / `llm_risk`（`message_id` で同定）にも同じ横断チェックを適用する

### 6.2 クールダウンとフィードバック学習

- `feedback = 'later'`（あとで）: その行を `dismissed` にし、`remind_after = now + 2日` を書く。2日後のハートビートで条件が継続していれば §6.1 の表に従って `active` に戻る
- `feedback = 'not_helpful'`（これは問題ない）: その行を `suppressed` にし、`remind_after = now + 30日` を書く（同一 `detector × 対象` を30日抑止）
- フィードバックは抑止にのみ使い、本人以外への表示・集計には使わない（§2.3）
- **（後続フェーズ）** 「同じ detector に not_helpful が続くユーザーには detector の閾値自体を緩める（例: due_soon を3日前→1日前に）」という個人適応は、回数・集計期間・保存先の設計が要るため Phase 1 では実装しない。Phase 1 は上記の 30日抑止（`remind_after`）までとする

### 6.3 頻度上限・静寂時間帯

- 1ユーザー1日 3件 まで。超過分は**破棄**する（翌日に持ち越さない。持ち越すと古い指摘が溜まって精度が落ちる）
- 静寂時間帯（初期値 22:00–08:00 JST）は配信しない。ハートビート実行時刻を朝に置くことで自然に回避しつつ、Phase 2 の高頻度巡回では配信遅延で対応する
- 優先順位はルール検知 > LLM検知（確実な指摘を優先枠に入れる）

---

## 7. 権限・プライバシー

- **AIの閲覧範囲は権限モデルに従う**。巡回はサービスロールで行うが、ナッジ生成時に「宛先ユーザーがその対象にアクセスできること」を `requireChannelAccess` / `requireProjectAccess` と同等の判定で必ず検証する。AI経由で見えないはずの情報が漏れる経路を作らない
- **DM は巡回しない**（N-4）。私的な会話をAIが読んでいるという感覚は、公開チャンネルの巡回とは受け入れられ方が質的に異なるため、初期スコープから除外する
- **非活性メンバーにはナッジを送らない**。宛先解決は `active_workspace_members` ビューを経由する（CLAUDE.md の認可規約に準拠）
- **ゲスト**は参加プロジェクトのタスク・チャンネルに関するナッジのみ受け取る
- `ai_nudges` の読み取りは `user_id = auth.uid()` 本人のみ（API層 + 将来のRLS双方で強制）。owner / admin にも例外を作らない
- **`user_id = auth.uid()` だけでは不十分**。生成時のアクセス検証（本節冒頭）はナッジ作成時点のスナップショットに過ぎず、その後に宛先ユーザーが非活性化・ゲストプロジェクトから除外・プライベートチャンネルから削除された場合、`user_id` 一致のみのポリシーでは古いナッジの `body` / `reason` を読めてしまう。既存のチャンネル閲覧が `requireChannelAccess` / `can_access_channel` で毎回アクセスを再評価しているのと同様に、**ナッジの読み取り（一覧取得API・RLS）でも現在の active membership + channel/project アクセスを都度再チェックする**。アクセスを失ったユーザーに紐づくナッジは、読み取り時に除外するか、失効イベント発生時に `status: 'suppressed'` へ遷移させて即座に無効化する

---

## 8. 配信・UI設計

### 8.1 チャット欄への合成表示

- ナッジは該当チャンネルのタイムライン上に、通常メッセージと視覚的に区別されたカードで表示する
  - AIエージェントのアイコン + 「このメッセージはあなただけに見えています」の常時表示
  - 本文 + 対象へのリンク（タスクを開く / 該当メッセージへジャンプ）
  - アクション: 「あとで」「これは問題ない」（+ タスク系は「完了にする」ショートカット）
- タイムライン上の位置は「配信時点の最新位置」。過去ログを遡ったときに埋もれないよう、チャンネル上部に未応答ナッジのピン表示も検討する（未決事項）
- チャンネルに紐づかないナッジ（例: プロジェクトチャンネルが無い場合）は通知ベルのみに出す

### 8.2 Realtime・通知との関係

- 配信は既存方式に従う: `ai_nudges` への **INSERT・UPDATE 双方**に DB トリガーを張り、`realtime.broadcast_changes()` で**本人の user topic** に配信 → `RealtimeProvider` がナッジクエリを invalidate → REST 再取得。ポーリングは行わない
  - UPDATE も対象にするのは、ハートビートがタスク完了・期限変更を検知して `status: 'active' → 'resolved'` にする（§4.1）、フィードバックで `suppressed` にする、といった更新を開きっぱなしのチャット画面にも反映するため。INSERT のみだと、解消済みのナッジカードが無関係な再取得・再接続まで表示され続けてしまう
- 実装時の注意: `RealtimeProvider`（`apps/web/src/components/realtime/realtime-provider.tsx`）のユーザートピック分岐は現状 `table === 'notifications'` / `table === 'channel_read_states'` のみを処理しており、`ai_nudges` は素通りする。`table === 'ai_nudges'` の分岐を追加してナッジ用クエリキーを invalidate しない限り、開きっぱなしのチャット画面に新規ナッジも状態変化も反映されない
- アプリ内通知（ベル）には `notification_type = 'ai'`（enum 定義済み）で記録し、チャットを開いていなくても後から回収できるようにする
  - **ベル通知はナッジと同じライフサイクルに従わせる**。`notifications` の一覧 API（`apps/web/src/app/api/notifications/route.ts`）は `user_id` / `workspace_id` でしかフィルタせず `body` をそのまま返すため、§7 のアクセス失効処理をナッジ側だけで行うと、ベルに複製された本文が失効後も読める迂回路になる。対策として `notifications.data` に `nudgeId` を持たせ、**`ai_nudges.status` が `suppressed` に遷移した時点（理由を問わない）で対応する通知行も削除する**共通フックとして実装する。読み取り時にチャンネル/プロジェクトアクセスを都度再評価する案もあるが、通知一覧は横断的で高頻度なため、失効イベント時のクリーンアップ方式を基本とする
  - この共通フックにより、アクセス失効（§7）だけでなくキルスイッチ ON（§8.3）も同じ経路でベル通知を消せる。両者を別々の削除ロジックとして実装しない
- **Push は初期は送らない**。ナッジは「開いたときにそっと目に入る」のが適切な強度であり、Push で割り込む価値がある確証を得てから解放する

### 8.3 個人キルスイッチ（Phase 1 必須）

ナッジ機能の全体テーゼが「うるさくない・監視しない（don't be evil）」である以上、**うるさいと感じた本人が自分で止められない状態で出荷してはならない**。よって最小のオフスイッチを Phase 1 の必須要件とする（F-11）。

- **最小構成**: ユーザー単位の boolean 1つ。`profiles` に `ai_nudges_enabled boolean NOT NULL DEFAULT true` を1カラム追加する（専用のユーザー設定テーブルは現状無いため、プロフィール系カラムに揃える）
- **サーバー側で尊重する**: ハートビートが**生成・配信の前に**このフラグを見て、OFF のユーザーはナッジ生成対象から外す（UI で隠すだけにしない）
- **トグル OFF は即時遡及する**: `ai_nudges_enabled` を `false` に更新するトランザクションの中で、そのユーザーの `active` / `dismissed` 状態にある既存ナッジを全て `suppressed` に遷移させる。これにより §8.2 の共通フック（`suppressed` 遷移時の通知行削除）がそのまま働き、チャット欄・ベルの両方から既存の未応答ナッジが即座に消える。専用の削除処理を別途持たない
- **UI**: 既存の設定・通知セクション（`apps/web/src/components/app/pages/settings.tsx`）に1トグルを追加する
- **今回やらないカスタマイズ**（後続フェーズ）: 頻度上限・静寂時間帯・detector 別 ON/OFF・ワークスペース単位/プロジェクト単位の粒度。Phase 1 は「全部 ON か 全部 OFF か」の1段だけ

---

## 9. 実装フェーズ

| Phase | 内容 | 完了条件 |
|---|---|---|
| 1 | `ai_nudges` テーブル + ルール検知3種 + ハートビート + チャット合成表示 + フィードバック + 個人キルスイッチ（§8.3） | 期限3日前の未着手タスクの担当者に、本人だけに見えるナッジが届き、タスク完了で自動 resolve される。キルスイッチ ON で以後一切届かない |
| 2 | LLM巡回（`ai_scan_states` + 差分スキャン + 二段階判定）+ 発話ゲート完全版 | 1〜2週間の実運用で「役に立った」フィードバックが「問題ない（誤指摘）」を上回る |
| 3 | メッセージ埋め込み（`sourceType: 'message'`）+ 文脈付きナッジ | ナッジに過去の議論への参照が正しく付く |
| 4 | 公開発言への拡張（[`10_ai_member_design.md`](./10_ai_member_design.md) Stage 3 本体へ合流）。第一候補は「結論が出ないまま流れた議論の要約再浮上」（例:「3日経ったので再浮上させます。論点はAかBです」——誰も責めないためパブリックでも成立しやすい） | — |

Phase 1 を最初に置くのは、LLM を使わずに誤指摘ほぼゼロで「裏方PMO」の体験・配信基盤・発話ゲートを検証できるため。**検知の賢さより先に、介入の作法（頻度・口調・言わない判断）を安いやり方で確立する。**

---

## 10. 未決事項

- ナッジの文面トーン（AGENTS.md の人格定義とどう接続するか。Phase 1 は固定文面でよい）
- ナッジ設定の粒度拡張（頻度上限・静寂時間帯・detector 別・ワークスペース/プロジェクト単位）をどの段階で出すか。**個人単位の一括オフは Phase 1 必須として確定**（§8.3 / F-11）。それ以外のカスタマイズは後続
- タイムライン合成の位置仕様（未応答ナッジのピン表示・古いナッジの自動折りたたみ）
- `task_stalled` の「停滞」判定に チャットでの言及 を加味するか（Phase 2 のLLM判定と統合する案）
- プロジェクト全体のリスク（期限直前×未完了タスク多数）をリーダーに知らせるか。「本人以外に見せない」原則との整理が必要（リーダー自身が行動主体である、という整理は可能）
- モバイル（Expo ネイティブチャット）での合成表示の実装時期
