同階層の CLAUDE.md を読んで、その指示に従うこと

## ユーザーの許可と自動承認レビュー

- 明示的な依頼・許可は意味で判断し、定型句や完全一致を要求しない。
- 許可済みの操作が自動承認レビューで拒否された場合、ユーザーの許可不足と混同せず、拒否理由を説明する。
- 同じ確認を繰り返さず、新しい判断材料がなければ拒否された操作を再試行せず、ユーザーの返信を待つ。拒否を別の手段で回避しない。

## `/chats` とアプリ版

Web の `/chats`（一覧と `/chats/[channelId]`）を変えるときは、Expo のネイティブチャットにも同じ変更が必要か確認する。チャットは WebView ではなく `apps/mobile`（入口は `apps/mobile/app/(app)/chats/`）の実装である。会話一覧、メッセージ、返信、メンション、添付、未読、Realtime など、アプリ利用者にも届く挙動なら、同じ変更に含めて直す。Web のシェルやキーボードショートカットだけのときはアプリは触らない。その判断は変更の説明に残す。
