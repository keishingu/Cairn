// RFC 9728 path-aware discovery URL. The WWW-Authenticate challenge points at the shorter
// compatibility URL, and both return the same protected resource metadata.
export { GET, OPTIONS } from '../../route'
